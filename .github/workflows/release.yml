# description of this workflow, can be anything you want
name: WOW Packager (WOWInterface/CurseForge/Wago.io)

# Triggers:
#   - Automatically when a v* tag is pushed manually
#   - Via workflow_dispatch (called by toc-update.yml after a TOC bump)
on:
  push:
    tags:
      - '!v*.*.*-*'
      - '!v*.*-*'
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version being released (e.g. v6.18) — informational only'
        required: false
        default: ''

# a workflow is built up as jobs, and within these jobs are steps
jobs:

  # "release" is a job, you can name it anything you want
  release:

    # we can run our steps on pretty much anything, but the "ubuntu-latest" image is a safe bet
    runs-on: ubuntu-latest

    # specify the environment variables used by the packager, matching the secrets from the project on GitHub
    env:
      CF_API_KEY: ${{ secrets.CF_API_KEY }}
      WOWI_API_TOKEN: ${{ secrets.WOWI_API_TOKEN }}
      WAGO_API_TOKEN: ${{ secrets.WAGO_API_TOKEN }}
      GITHUB_OAUTH: ${{ secrets.GITHUB_TOKEN }}
      # "GITHUB_TOKEN" is a secret always provided to the workflow
      # for your own token, the name cannot start with "GITHUB_"

    # "steps" holds a list of all the steps needed to package and release our AddOn
    steps:
      # we first have to clone the AddOn project, this is a required step
      - name: Clone project
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Package and release (auto-detects supported game versions from TOC)
      - name: Package and release
        uses: BigWigsMods/packager@v2
